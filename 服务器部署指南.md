# 服务器部署指南 v0.2.6

## 🚀 一键部署（推荐）

### 方法1：使用一键部署脚本

```bash
# 在服务器上执行
cd ~/DA/Analysis_A
bash 一键部署.sh
```

**脚本会自动完成**：
1. ✅ 备份重要配置文件（database.py、状态文件）
2. ✅ 拉取最新代码
3. ✅ 检查并恢复配置文件（防止被覆盖）
4. ✅ 检查前端构建文件
5. ✅ 清除Python缓存
6. ✅ 停止旧服务
7. ✅ 启动新服务
8. ✅ 验证服务状态

---

## 📋 手动部署（备选）

### 步骤1：备份配置

```bash
cd ~/DA/Analysis_A

# 备份数据库配置
cp backend/app/database.py ~/database.py.backup

# 备份数据状态
cp data/data_import_state.json ~/data_import_state.json.backup
cp data/sector_import_state.json ~/sector_import_state.json.backup
```

### 步骤2：拉取代码

```bash
git pull origin main
```

### 步骤3：检查配置

```bash
# 检查database.py
cat backend/app/database.py | grep DB_HOST

# 如果看到"192.168.182.128"说明被覆盖，恢复备份：
cp ~/database.py.backup backend/app/database.py
```

### 步骤4：清除缓存

```bash
find backend -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null
find backend -name "*.pyc" -delete
```

### 步骤5：重启服务

```bash
bash stop.sh
bash start_all.sh
```

### 步骤6：验证

```bash
# 检查后端
curl http://localhost:8000/api/dates

# 检查Nginx代理
curl http://localhost/api/dates

# 查看日志
tail -f logs/backend.log
```

---

## ⚠️ 重要提示

### 必须检查的配置文件

| 文件 | 说明 | 检查方法 |
|-----|------|---------|
| `backend/app/database.py` | 数据库配置 | `grep DB_HOST backend/app/database.py` |
| `data/data_import_state.json` | 数据导入状态 | 文件大小应该 > 100字节 |
| `frontend/build/` | 前端构建文件 | `ls frontend/build/` 应有文件 |

### 常见问题

#### 问题1：git pull后服务无法启动

**原因**：`database.py`被覆盖为本地配置

**解决**：
```bash
cp ~/database.py.backup backend/app/database.py
bash stop.sh && bash start_all.sh
```

#### 问题2：前端显示404

**原因**：`frontend/build`未更新

**解决**：
```bash
# 本地执行
cd frontend
npm run build
git add frontend/build
git commit -m "build: 更新前端"
git push

# 服务器执行
git pull
sudo systemctl reload nginx
```

#### 问题3：配置不生效

**原因**：Python缓存未清除

**解决**：
```bash
find backend -name __pycache__ -delete
bash stop.sh && bash start_all.sh
```

---

## 📝 验证清单

### 部署后必须验证

- [ ] 后端服务运行：`ps aux | grep uvicorn`
- [ ] API响应正常：`curl http://localhost:8000/api/dates`
- [ ] Nginx代理正常：`curl http://localhost/api/dates`
- [ ] 前端可访问：浏览器访问 http://60.205.251.109
- [ ] 数据库配置正确：`grep DB_HOST backend/app/database.py`
- [ ] 无错误日志：`tail -100 logs/backend.log | grep ERROR`

---

## 🔄 回滚操作

如果部署后出现问题，立即回滚：

```bash
# 方式1：Git回滚
cd ~/DA/Analysis_A
git log --oneline -5  # 查看提交历史
git reset --hard <上一个commit-id>
bash stop.sh && bash start_all.sh

# 方式2：恢复备份配置
cp ~/database.py.backup backend/app/database.py
cp ~/data_import_state.json.backup data/data_import_state.json
bash stop.sh && bash start_all.sh
```

---

## 📞 常用命令

### 服务管理

```bash
# 启动所有服务
bash start_all.sh

# 停止所有服务
bash stop.sh

# 查看运行状态
ps aux | grep uvicorn

# 查看日志
tail -f logs/backend.log
```

### Nginx管理

```bash
# 测试配置
sudo nginx -t

# 重新加载配置
sudo systemctl reload nginx

# 重启Nginx
sudo systemctl restart nginx

# 查看错误日志
sudo tail -f /var/log/nginx/error.log
```

### 数据库相关

```bash
# 连接数据库
psql -h localhost -U postgres -d db_20251106_analysis_a

# 查看数据量
psql -h localhost -U postgres -d db_20251106_analysis_a -c "SELECT COUNT(*) FROM daily_stock_data;"

# 清空数据（谨慎！）
psql -h localhost -U postgres -d db_20251106_analysis_a -c "TRUNCATE TABLE daily_stock_data CASCADE;"
```

---

## 🎯 首次部署（全新服务器）

如果是第一次部署到新服务器，请参考：`docs/⚠️重要-配置文件管理和常见错误.md` 第572-693行

**核心步骤**：
1. 克隆代码
2. 创建`backend/app/database.py`（参考`.example`）
3. 配置Nginx
4. 启动服务

---

## 📚 参考文档

- **配置管理**：`docs/⚠️重要-配置文件管理和常见错误.md`
- **版本说明**：`docs/v0.2.6-上线总结.md`
- **上线检查**：`上线前最终检查.md`

---

## 🆘 紧急联系

如果遇到无法解决的问题：

1. 查看错误日志：`tail -100 logs/backend.log`
2. 查看Nginx日志：`sudo tail -50 /var/log/nginx/error.log`
3. 检查进程状态：`ps aux | grep uvicorn`
4. 查看部署文档中的"问题排查"部分

---

**最后更新时间**: 2025-11-09  
**当前版本**: v0.2.6  
**维护人员**: 开发团队

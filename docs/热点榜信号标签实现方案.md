# 热点榜信号标签实现方案

## 需求概述

热点榜信号支持两种模式，可在"信号阈值配置"中切换：

### 1. 总分TOP信号（原版）
- **适用场景**：快速筛选当日综合得分龙头股票
- **信号格式**：`热点榜TOP100`、`热点榜TOP500`
- **判断依据**：基于当日综合评分排名
- **权重**：25%（固定）

### 2. 最新热点TOP信号（新版）
- **适用场景**：发现持续出现的强势热点股票  
- **信号格式**：`TOP100·5次`（区间·出现次数）
- **判断依据**：基于"最新热点"模块14天聚合数据
- **权重**：25%基础，根据出现次数微调（12次×1.2、10次×1.1、8次×1.05）
- **固定区间**：TOP100/200/400/600/800/1000（不可调节）

---

## 🎯 功能目标

### 功能1：信号标签系统
为其他模块的股票打上热点榜信号标签：

| 排名区间 | 信号标签 | 说明 |
|---------|---------|------|
| 1-100 | 🔥 热点榜TOP100 | 最热门 |
| 101-200 | 🔥 热点榜TOP200 | 很热门 |
| 201-400 | 🔥 热点榜TOP400 | 较热门 |
| 401-600 | 🔥 热点榜TOP600 | 中等热度 |
| 601-800 | 🔥 热点榜TOP800 | 一般热度 |
| 801-1000 | 🔥 热点榜TOP1000 | 低热度 |

**显示优先级**：100 > 200 > 400 > 600 > 800 > 1000

**应用场景**：
- 板块详情页：显示成分股的热点榜标签
- 股票查询页：显示查询股票的热点榜标签
- 排名跳变榜：显示跳变股票的热点榜标签
- 稳步上升榜：显示上升股票的热点榜标签

### 功能2：搜索功能
在"最新热点"模块添加搜索功能：
- 输入股票代码/名称
- 模糊匹配搜索
- 高亮显示搜索结果
- 保持原始排名（不因过滤改变）
- 未找到时提示文案

---

## 🏗️ 技术架构

### 整体架构图

```
┌─────────────────────────────────────────────────────┐
│                   应用启动                           │
│              加载热点榜数据缓存                      │
└─────────────────┬───────────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        ↓                   ↓
┌──────────────────┐  ┌──────────────────┐
│  HotSpotsCache   │  │  热点榜API       │
│  按日期缓存      │  │  返回完整数据    │
│  {日期: {        │  │  用于前端搜索    │
│    data: [...],  │  │                  │
│    rank_map: {}  │  │                  │
│  }}              │  │                  │
└─────────┬────────┘  └─────────┬────────┘
          │                     │
          ↓                     ↓
┌──────────────────┐  ┌──────────────────┐
│ SignalCalculator │  │ 前端搜索组件     │
│ 查询股票排名     │  │ 模糊过滤        │
│ 返回信号标签     │  │ 高亮显示        │
└─────────┬────────┘  └──────────────────┘
          │
          ↓
┌──────────────────┐
│ 各模块集成       │
│ 显示信号标签     │
└──────────────────┘
```

---

## 💾 数据结构设计

### 1. 缓存结构 (HotSpotsCache)

```python
{
    "20251107": {                    # 日期键
        "data": [                    # 完整榜单（前端展示用）
            {
                "code": "920961",
                "name": "创远信科",
                "rank": 1,           # 排名
                "hit_count": 12,     # 14天命中次数
                "latest_date": "20251107",
                "industry": "通信设备",
                "change_pct": 5.67,
                ...
            },
            ...  # 1000条
        ],
        "rank_map": {                # 快速查询映射（信号计算用）
            "920961": 1,
            "300394": 2,
            ...  # 1000个
        },
        "timestamp": 1699603200      # 缓存时间戳
    },
    "20251106": { ... },             # 其他日期
    ...
}
```

### 2. API响应格式

#### GET /api/hot-spots/full
```json
{
    "date": "20251107",
    "total_count": 1000,
    "stocks": [
        {
            "code": "920961",
            "name": "创远信科",
            "rank": 1,
            "rank_label": "热点榜TOP100",
            "hit_count": 12,
            "latest_date": "20251107",
            "industry": "通信设备",
            "change_pct": 5.67
        }
    ]
}
```

---

## 🔧 核心实现

### 1. 缓存管理器 (hot_spots_cache.py)

**职责**：
- 按日期缓存热点榜数据
- 提供快速排名查询（O(1)）
- 提供完整数据获取（前端展示）
- TTL过期管理

**核心方法**：
```python
class HotSpotsCache:
    @classmethod
    def get_rank(cls, stock_code: str, date: str) -> Optional[int]:
        """获取股票在指定日期的排名（用于信号计算）"""
        
    @classmethod
    def get_full_data(cls, date: str) -> List[dict]:
        """获取完整榜单数据（用于前端展示）"""
        
    @classmethod
    def _load_date(cls, date: str):
        """从数据库加载数据并构建缓存"""
```

**TTL策略**：
- 缓存过期时间：24小时
- 自动清理超过30天的缓存
- 应用启动时预加载最近3天

---

### 2. 信号计算器扩展 (signal_calculator.py)

**新增方法**：
```python
def calculate_hot_spot_signal(
    self, 
    stock_code: str, 
    date: str
) -> Optional[str]:
    """计算热点榜信号
    
    Args:
        stock_code: 股票代码
        date: 日期 (YYYYMMDD)
        
    Returns:
        "热点榜TOP100" | "热点榜TOP200" | ... | None
    """
    rank = HotSpotsCache.get_rank(stock_code, date)
    
    if not rank:
        return None
    
    # 按区间返回标签
    if rank <= 100:
        return "热点榜TOP100"
    elif rank <= 200:
        return "热点榜TOP200"
    elif rank <= 400:
        return "热点榜TOP400"
    elif rank <= 600:
        return "热点榜TOP600"
    elif rank <= 800:
        return "热点榜TOP800"
    elif rank <= 1000:
        return "热点榜TOP1000"
    
    return None
```

**集成到现有流程**：
```python
def calculate_signals_for_stock(
    self,
    stock_code: str,
    stock_data: dict,
    date: str,
    thresholds: SignalThresholds
) -> List[str]:
    """计算某只股票的所有信号"""
    signals = []
    
    # 1. 热点榜信号（最高优先级）
    hot_signal = self.calculate_hot_spot_signal(stock_code, date)
    if hot_signal:
        signals.append(hot_signal)
    
    # 2. 其他信号（跳变、涨幅、换手率等）
    # ... 现有逻辑
    
    return signals
```

---

### 3. API接口修改

#### 修改 /api/hot-spots
添加`rank_label`字段：

```python
@router.get("/hot-spots/full")
async def get_hot_spots_full(
    date: str = Query(default=None),
    days: int = Query(default=14),
    top_n: int = Query(default=1000)
):
    target_date = date or get_latest_date()
    
    # 从缓存获取
    data = HotSpotsCache.get_full_data(target_date)
    
    # 添加rank_label
    for idx, stock in enumerate(data):
        rank = idx + 1
        stock["rank_label"] = get_rank_label(rank)
    
    return {
        "date": target_date,
        "total_count": len(data),
        "stocks": data[:top_n]
    }
```

#### 修改板块详情API
```python
@router.get("/api/industry/{industry_name}/stocks")
async def get_industry_stocks(
    industry_name: str,
    date: str = Query(default=None),
    sort_mode: str = Query(default="hot"),
    # 信号阈值参数
    calculate_signals: bool = Query(default=True),
    ...
):
    # ... 现有逻辑
    
    # 为每只股票计算信号（包含热点榜信号）
    if calculate_signals:
        for stock in stocks:
            stock["signals"] = signal_calculator.calculate_signals_for_stock(
                stock["code"],
                stock,
                target_date,
                thresholds
            )
    
    return {"stocks": stocks}
```

---

### 4. 前端搜索组件

#### HotSpotsModule.js

```jsx
function HotSpotsModule() {
  const [allStocks, setAllStocks] = useState([]);      // 完整数据（1000条）
  const [displayStocks, setDisplayStocks] = useState([]);  // 显示数据
  const [searchQuery, setSearchQuery] = useState("");
  
  // 加载数据
  useEffect(() => {
    loadHotSpots();
  }, [selectedDate]);
  
  const loadHotSpots = async () => {
    const response = await axios.get(
      `${API_BASE_URL}/api/hot-spots/full?date=${selectedDate}&days=14&top_n=1000`
    );
    setAllStocks(response.data.stocks);
    setDisplayStocks(response.data.stocks.slice(0, topNFilter));
  };
  
  // 搜索过滤（前端实现，模糊匹配）
  const handleSearch = (query) => {
    setSearchQuery(query);
    
    if (!query.trim()) {
      // 无搜索：显示前N条
      setDisplayStocks(allStocks.slice(0, topNFilter));
      return;
    }
    
    // 模糊搜索：代码或名称包含关键词
    const filtered = allStocks.filter(stock => 
      stock.code.includes(query) || 
      stock.name.includes(query.toUpperCase())
    );
    
    setDisplayStocks(filtered);
  };
  
  return (
    <div className="hot-spots-module">
      {/* 搜索框 */}
      <SearchBar 
        value={searchQuery}
        onChange={handleSearch}
        placeholder="搜索股票代码或名称..."
      />
      
      {/* 搜索结果提示 */}
      {searchQuery && displayStocks.length === 0 && (
        <EmptyState message={`未找到包含"${searchQuery}"的股票`} />
      )}
      
      {searchQuery && displayStocks.length > 0 && (
        <ResultHint count={displayStocks.length} />
      )}
      
      {/* 股票列表（高亮显示） */}
      <StockList 
        stocks={displayStocks}
        highlight={searchQuery}
      />
    </div>
  );
}
```

#### 高亮组件

```jsx
function HighlightText({ text, highlight }) {
  if (!highlight) return text;
  
  const parts = text.split(new RegExp(`(${highlight})`, 'gi'));
  
  return (
    <>
      {parts.map((part, i) => 
        part.toLowerCase() === highlight.toLowerCase() 
          ? <mark key={i} className="highlight">{part}</mark>
          : part
      )}
    </>
  );
}
```

---

## 📊 标签格式设计

### 最终标签格式
使用中点符号（·）连接，简洁美观：
```
TOP100·12次
TOP200·8次
TOP400·5次
TOP600·4次
TOP800·3次
TOP1000·2次
```

### 设计理由
1. **双重指标**：排名 + 次数，信息更完整
2. **简洁性**：去掉"热点榜"前缀，节省空间
3. **美观性**：中点符号比冒号更柔和
4. **可读性**：一眼就能看出排名区间和热度

### 次数加权
出现次数越多，信号分数越高：
- ≥12次：×1.2（持续热点）
- ≥10次：×1.1（稳定热点）
- ≥8次：×1.05（一般热点）
- <8次：不加权

---

## 🎨 UI设计

### 信号标签样式

```css
/* 热点榜标签 - 渐变背景 */
.signal-badge.hot-top100 {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  font-weight: 600;
}

.signal-badge.hot-top200 {
  background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  color: white;
}

.signal-badge.hot-top400 {
  background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
  color: white;
}

.signal-badge.hot-top600 {
  background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
  color: white;
}

.signal-badge.hot-top800 {
  background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
  color: #333;
}

.signal-badge.hot-top1000 {
  background: #f0f0f0;
  color: #666;
  border: 1px solid #e0e0e0;
}
```

### 搜索高亮样式

```css
/* 搜索高亮 */
.highlight {
  background-color: #fff3cd;
  color: #856404;
  font-weight: 600;
  padding: 0 2px;
  border-radius: 2px;
}

/* 搜索框 */
.search-bar {
  position: relative;
  margin-bottom: 16px;
}

.search-bar input {
  width: 100%;
  padding: 10px 36px 10px 36px;
  border: 2px solid #e0e0e0;
  border-radius: 8px;
  font-size: 14px;
  transition: border-color 0.3s;
}

.search-bar input:focus {
  outline: none;
  border-color: #667eea;
}

/* 空状态 */
.search-empty {
  text-align: center;
  padding: 60px 20px;
  color: #999;
}

.search-empty svg {
  margin-bottom: 12px;
}

.search-empty .hint {
  font-size: 13px;
  color: #ccc;
  margin-top: 8px;
}
```

---

## 📊 性能优化

### 1. 缓存策略
- **内存缓存**：热点榜数据（1000条 × 100字节 ≈ 100KB/天）
- **缓存容量**：最多保留30天历史数据（≈ 3MB）
- **预加载**：应用启动时加载最近3天
- **延迟加载**：历史日期按需加载

### 2. 查询优化
- **O(1)查询**：使用HashMap存储排名映射
- **批量计算**：一次性计算所有股票的信号
- **前端过滤**：搜索功能在前端实现，无需后端请求

### 3. 网络优化
- **一次加载**：前端加载完整TOP1000数据
- **前端缓存**：避免重复请求
- **增量更新**：只在日期变化时重新加载

---

## 🧪 测试计划

### 单元测试

1. **HotSpotsCache测试**
   - 测试数据加载
   - 测试排名查询
   - 测试TTL过期
   - 测试多日期缓存

2. **SignalCalculator测试**
   - 测试6个区间判断
   - 测试不存在的股票
   - 测试边界值（100, 200, 400...）

3. **前端搜索测试**
   - 测试模糊匹配
   - 测试高亮显示
   - 测试空结果
   - 测试排名保持

### 集成测试

1. **API测试**
   - 测试 `/api/hot-spots/full`
   - 测试板块详情信号标签
   - 测试历史日期查询

2. **端到端测试**
   - 测试完整搜索流程
   - 测试信号标签显示
   - 测试多页面集成

---

## 📅 实施计划

### Phase 1: 后端核心（3小时）
1. ✅ 创建 `HotSpotsCache` 类
2. ✅ 实现缓存加载逻辑
3. ✅ 实现排名查询接口
4. ✅ 扩展 `SignalCalculator`
5. ✅ 修改热点榜API

### Phase 2: API集成（2小时）
1. ✅ 修改板块详情API
2. ✅ 修改股票查询API
3. ✅ 添加信号计算逻辑
4. ✅ 测试API返回

### Phase 3: 前端搜索（2小时）
1. ✅ 添加搜索框组件
2. ✅ 实现模糊过滤
3. ✅ 实现高亮显示
4. ✅ 添加空状态提示

### Phase 4: 前端集成（1.5小时）
1. ✅ 板块详情页显示标签
2. ✅ 其他页面显示标签
3. ✅ 优化标签样式
4. ✅ 测试响应式

### Phase 5: 测试优化（1.5小时）
1. ✅ 单元测试
2. ✅ 集成测试
3. ✅ 性能测试
4. ✅ 文档完善

**总计：10小时**

---

## 🔄 与现有系统对比

### 旧方案（热点榜阈值配置）
```
❌ 问题：
- 基于各模块自身排名判断
- 需要用户配置阈值（TOP100/200...）
- 没有实际意义（本身就是排序结果）
- 无法体现真实热度

✅ 优点：
- 实现简单
```

### 新方案（热点榜排名信号）
```
✅ 优点：
- 基于14天聚合热点榜
- 固定6个区间，无需配置
- 真实反映股票热度
- 支持搜索功能

⚠️ 注意：
- 需要缓存管理
- 增加一定复杂度
```

---

## 📝 配置变更

### 移除配置项
从 `SignalConfigPanel` 中移除"热点榜阈值"配置：
- ❌ 移除：热点榜TOP100/200/400...选项
- ✅ 保留：其他信号阈值配置（跳变、涨幅等）

### 配置说明更新
```
旧：
• 热点榜阈值：进入热点榜前X名

新：
（无需配置，自动基于14天热点榜排名）
```

---

## 🎯 预期效果

### 信号标签展示
```
板块详情页：
┌─────────────────────────────────────────┐
│ 股票代码  │ 股票名称  │ 信号标签        │
├─────────────────────────────────────────┤
│ 920961   │ 创远信科  │ 🔥热点榜TOP100  │
│          │          │ 📈跳变3424名     │
│          │          │ 💰涨幅12.7%      │
└─────────────────────────────────────────┘
```

### 搜索功能展示
```
最新热点模块：
┌──────────────────────────────────┐
│ 🔍 [创远]______________ [×]      │  ← 搜索框
├──────────────────────────────────┤
│ 找到 1 只股票，显示原始排名       │  ← 提示
├──────────────────────────────────┤
│ #1  920961  创远信科  🔥TOP100   │  ← 高亮显示
│     命中12次  最近:2025-11-07    │
└──────────────────────────────────┘
```

---

## 🚀 上线checklist

- [ ] 后端代码完成
- [ ] 前端代码完成
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 性能测试通过
- [ ] 文档更新完成
- [ ] 配置项移除
- [ ] 前端build重新编译
- [ ] 代码review
- [ ] 部署到测试环境
- [ ] 功能验收
- [ ] 部署到生产环境

---

**文档版本**: v1.0  
**创建时间**: 2025-11-10  
**最后更新**: 2025-11-10  
**作者**: 开发团队

# 前端模块化重构指南

## 🎯 重构目标

将1879行的`App.js`拆分成独立的功能模块组件。

## 📁 目录结构

```
frontend/src/
├── App.js                    # 主应用（简化版）
├── components/
│   ├── common/              # 通用组件（已存在）
│   │   ├── Sidebar.js
│   │   ├── Loading.js
│   │   └── ErrorMessage.js
│   └── modules/             # 功能模块（需创建）
│       ├── index.js
│       ├── HotSpotsModule.js      # 最新热点
│       ├── StockQueryModule.js    # 股票查询
│       ├── IndustryTrendModule.js # 行业趋势
│       ├── RankJumpModule.js      # 排名跳变
│       └── SteadyRiseModule.js    # 稳步上升
├── hooks/                   # 自定义hooks（已存在）
├── services/                # API服务（已存在）
├── constants/               # 常量配置（已存在）
└── utils/                   # 工具函数（已存在）
```

## 🔧 重构步骤

### 方案1：手动重构（推荐，更可控）

1. **备份当前App.js**
   ```bash
   cp frontend/src/App.js frontend/src/App.js.backup
   ```

2. **从App.js中提取每个模块的代码到独立文件**
   - 最新热点：行12-20的状态 + 相关useEffect + 相关渲染逻辑
   - 股票查询：行22-34的状态 + 相关逻辑
   - 行业趋势：行36-44的状态 + 相关逻辑  
   - 排名跳变：行46-54的状态 + 相关逻辑
   - 稳步上升：行56-65的状态 + 相关逻辑

3. **替换App.js为简化版**
   ```bash
   cp frontend/src/App.refactored.js frontend/src/App.js
   ```

### 方案2：使用脚本辅助（快速）

运行重构脚本（我可以为您生成）：
```bash
node frontend/scripts/refactor.js
```

## 📝 模块示例结构

每个模块组件应包含：

```javascript
import React, { useState, useEffect } from 'react';
import { API_BASE_URL, PERIODS } from '../../constants';
import { useAnalysis } from '../../hooks';
import { Loading, ErrorMessage } from '../common';

export default function HotSpotsModule() {
  // 本模块的状态
  const [boardType, setBoardType] = useState('main');
  const [selectedPeriod, setSelectedPeriod] = useState(2);
  
  // 使用自定义hooks
  const { data, loading, error } = useAnalysis(selectedPeriod, boardType);
  
  // 渲染逻辑
  return (
    <div>
      {/* 模块UI */}
    </div>
  );
}
```

## ✅ 重构后的优势

1. **代码组织清晰** - 每个模块独立文件
2. **易于维护** - 模块间互不干扰
3. **复用性强** - hooks和组件可复用
4. **性能优化** - 可按需加载模块
5. **团队协作** - 多人可同时开发不同模块

## 🚀 下一步

选择你喜欢的方式：

**A. 我逐个为你创建模块组件**（需要较多时间）
**B. 你手动拆分**（跟随上面的指南）
**C. 我生成一个自动化重构脚本**

请告诉我你选择哪种方式！

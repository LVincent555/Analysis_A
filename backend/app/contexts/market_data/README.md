# Market Data Context

Owns stocks, sectors, daily market data and import commands.

The first migration step is to introduce read repositories and import commands while keeping ORM models in the legacy aggregate until ownership is settled.
